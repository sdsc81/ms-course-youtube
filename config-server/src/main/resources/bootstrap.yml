#Application Configuration
server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/sdsc81/ms-course-youtube.git
          username: sdsc81
          password: Tgf$hdik634ldfugnd7j$nd
          default-label: "ciego-06-security"
          search-paths:
            - data-config
  security:
    user:
      name: root
      password: s3cr3t
  #logging:
    #level:
      #org:
        #springframework:
        #boot:
      #autoconfigure: DEBUG